# 1
```c
int none_pointer = 10;
int *pointer;

pointer = &none_pointer;  // pointer는 none_pointer의 주소를 저장
*pointer = 20;            // none_pointer의 값이 20으로 변경됩니다.
```

# 2
```c
int none_pointer = 10;
int *pointer = &none_pointer;  // pointer는 none_pointer의 주소를 저장
*pointer = 20;            // none_pointer의 값이 20으로 변경됩니다.
```

